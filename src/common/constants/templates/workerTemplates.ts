import * as C from "game/constants";

export const LOCALMINER = [C.CARRY, C.MOVE];
export const LOCALRELAY = [C.CARRY];
export const REMOTEBUILDER_TEMPLATE = [C.MOVE, C.MOVE, C.MOVE, C.MOVE, C.MOVE, C.CARRY, C.CARRY, C.WORK];
